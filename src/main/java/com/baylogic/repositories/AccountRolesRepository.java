package com.baylogic.repositories;

import org.springframework.data.jpa.repository.JpaRepository;

import com.baylogic.pojos.AccountRoles;

public interface AccountRolesRepository extends JpaRepository<AccountRoles, Integer> {
}
